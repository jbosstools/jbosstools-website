---
layout: project
tab: features
--- 

.row-fluid
  .span3.sidebar
    %ul.nav.nav-list
      - feature = site.features.values.select{|feature| feature.url == join(site.base_url, page.url)}.first
      %li.nav-header
        #{feature.name}
      %li.active 
        %a Overview
      %li
        %a Issue Tracker
      - unless site.whatsnews[page.feature_id].nil?
        %li 
          - latest_version = site.whatsnews[page.feature_id][:merged].keys.sort.last
          - latest_whatsnew_page = site.whatsnews[page.feature_id][:merged][latest_version]
          -# puts "Latest version: " + latest_whatsnew_page.to_s
          %a{:href=>latest_whatsnew_page.url } What's New 
      
      %li.divider
      %li.nav-header 
        Other features
      - feature = site.features.each do |feature_id, feature|
        - if feature.url != join(site.base_url, page.url)
          %li
            %a{:href => feature.url} #{feature.name}
    
  .span9#feature
    ~content

