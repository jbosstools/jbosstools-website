---
layout: default
tab: features
---
.row-fluid 
  .span12#main-features
    %h4<Feature Tour
    %hr
  .span12
    - features = site.features 
    - features = features.sort{|l,r| l.order <=> r.order}
    .features 
      - for feature in features
        %h4 
          %a{:href=>feature.url} 
            #{feature.title}
          %a{:href=>feature.url} 
            %img{:src=>"#{feature.image_url}", :alt=>"#{feature.title}"}
        %p 
          #{feature.summary}
          %br
          %a.readmore{:href=>feature.url} Read more » 
        %hr
   
  
  

