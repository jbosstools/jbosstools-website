---
layout: default_layout
tab: features
title: Features Overview
---

- features = site.features.sort{|l,r| l.order <=> r.order}
- highlighted_feature = features.shift

-# show first feature with a highlight
.row-fluid 
  .span12.hero-unit
    %img{:src=>"#{site.base_url}/#{highlighted_feature.image_url}", :alt=>"#{highlighted_feature.title}"}
    .hero-content
      %h1< #{highlighted_feature.title}
      %h2< #{highlighted_feature.tagline}
      #{highlighted_feature.summary}
    
.row-fluid 
  .span12
    %hr
      
-# show all other features
.row-fluid 
  .span12
    .features 
      - for feature in features
        %h4 
          %a{:href=>feature.url} 
            #{feature.title}
          %a{:href=>feature.url} 
            %img{:src=>"#{site.base_url}/#{feature.image_url}", :alt=>"#{feature.title}"}
        %p 
          #{feature.summary}
          %br
          %a.readmore{:href=>feature.url} Read more&hellip;
        %hr
   
  
  

